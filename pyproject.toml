[project]
name = "DiscordHoneypotDetector"
version = "0.1.0"
description = "Give a score to a message to determine if it is a honeypot warning or not"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "joblib>=1.5.3",
    "scikit-learn>=1.9.0",
    "sentence-transformers>=5.7.0",
]
